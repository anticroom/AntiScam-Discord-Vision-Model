## How it Works

This light weight model uses **MobileNetV3-Small** with **PyTorch**. The model is a 6MB binary classifier specifically for processing large amounts of media easily if used on larger scales like checking every image sent in a Discord server for any malicious scams the model specifically trained to detect.
